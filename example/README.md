# Example app

Demonstrates three integration patterns from the package README:

1. **Notifications** — `setState` + custom `SmoothToggleStyle` + `onToggle`
2. **Wi‑Fi** — `SmoothToggleController` + `ValueListenableBuilder`
3. **Dark mode** — `textInsideThumb: true`

```bash
flutter run
```
